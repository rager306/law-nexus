# Historical PRD/document vault

Not active product architecture or requirements truth. All subtrees below are
gitignored (except this README when force-added).

| Path | Contents | Lifecycle |
|------|----------|-----------|
| `acp-git-lex/` | ACP/git-lex era milestone docs | archive-only (R066) |
| `pre-rust-prd/` | Early PRD/review/recommendation docs | historical prior art |
| `milestone-proofs-era/` | M001–M003 FalkorDB/MiniMax/embedding proofs | historical |
| `research-era/` | FalkorDB/git-lex/ACP/MiniMax research notes | historical |
| `project-state-era/` | ACP project-state projections (except live roadmap) | historical |
| `architecture-era/` | Onion/ACP residual architecture process docs | historical |

**Keep active under `prd/architecture/`:** registry schema/items/edges, health/report,
claims ledger, remediation/track/closure artifacts, crate allowlist, m111 baseline,
`README.md`, and `acp` symlink (compat for historical fixtures).

**Active truth:** `prd/ARCHITECTURE.md`, `doc/adr/**`, current `prd/migration/**`
(including decommission roadmaps).

Do not promote claims from this vault without re-validation (D098).
