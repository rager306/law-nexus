# Parser Golden-Test Proof Report

> Non-authoritative inspection surface for M008/S04. This report summarizes bounded parser golden-test evidence from tracked `prd/parser/` artifacts and executable `--check` commands. It is not legal advice and is not a product-readiness claim.

## Scope

This report proves only that the current tracked parser golden-test chain is internally consistent for five bounded cases. The proof is limited to repository artifacts under `prd/parser/` plus the deterministic checker commands listed below.

It does not prove parser completeness, product retrieval quality, citation-safe retrieval readiness, legal-answer correctness, relation correctness, FalkorDB loading/runtime readiness, Consultant WordML legal authority, product ETL readiness, or product graph truth.

## Source artifacts

The proof is derived from these tracked artifacts:

- `prd/parser/golden_test_contract.md` — golden-test contract and explicit non-claim boundary.
- `prd/parser/golden_cases.json` — machine-readable generated golden cases.
- `prd/parser/golden_cases.md` — human-readable generated case inventory.
- `prd/parser/odt_document_records.jsonl` — bounded non-authoritative document fixtures.
- `prd/parser/odt_source_block_records.jsonl` — bounded non-authoritative source-block fixtures.
- `prd/parser/consultant_relation_candidates.jsonl` — candidate-only Consultant relation fixture.
- `prd/parser/parser_staging_graph.json` — staging graph diagnostics, including unresolved Consultant references.
- `scripts/build-parser-golden-cases.py` — generator/check command surface.
- `scripts/evaluate-parser-golden-cases.py` — evaluator/check command surface.

## Command evidence

Fresh local command evidence for this report:

```bash
uv run python scripts/build-parser-golden-cases.py --check
uv run python scripts/evaluate-parser-golden-cases.py --check
```

Observed public fields:

| Surface | Status | Case count | Evaluated | Errors | Warnings | Info | Non-authoritative |
| --- | --- | ---: | ---: | ---: | ---: | ---: | --- |
| build golden cases | `pass` | 5 | n/a | n/a | n/a | n/a | `true` |
| evaluate golden cases | `pass` | 5 | 5 | 0 | 1 | 2 | `true` |

Evaluator severity counts are `error: 0`, `warning: 1`, and `info: 2`. The evaluator schema is `legalgraph-parser-golden-evaluator/v1`, generated by `scripts/evaluate-parser-golden-cases.py`.

## Required case coverage

The current bounded proof covers exactly one case for each required class:

| Case | Class | Required bounded observation |
| --- | --- | --- |
| `GT-001` | `evidence-present` | Bounded evidence exists for `BLOCK-44-FZ-000` in `prd/parser/odt_source_block_records.jsonl`. |
| `GT-002` | `no-answer` | `DOC-ABSENT-GOLDEN-NO-ANSWER` remains intentionally absent with no fabricated evidence. |
| `GT-003` | `candidate-only` | `REL-CONS-0001` is preserved as `candidate-only` / `needs-review` evidence, not promoted to an authoritative relation. |
| `GT-004` | `unresolved-reference` | Consultant references remain unresolved and explicit: `consultant-list:law-source/consultant/Список документов (5).xml` and `consultant:LAW:179581@11.05.2026`. |
| `GT-005` | `non-authoritative` | The non-authoritative boundary and blocked claim labels are propagated. |

## Evaluator result

The exact current bounded proof is: five evaluated cases covering `evidence-present`, `no-answer`, `candidate-only`, `unresolved-reference`, and `non-authoritative`; evaluator status is `pass`; evaluated case count is 5; error count is 0.

The proof specifically preserves two important downstream boundaries:

- `GT-003` keeps `REL-CONS-0001` as candidate-only / needs-review and blocks relation-correctness, Consultant WordML legal-authority, and product-graph-truth claims.
- `GT-004` keeps unresolved Consultant references path-qualified in `prd/parser/parser_staging_graph.json` instead of rewriting them to ODT ids.

## Blocked claims and non-claims

The evaluator reports these blocked claims:

- `Consultant WordML legal authority`
- `FalkorDB loading/runtime readiness`
- `citation-safe retrieval readiness`
- `legal-answer correctness`
- `parser completeness`
- `product ETL readiness`
- `product graph truth`
- `relation correctness`
- `retrieval quality`

A pass only means the bounded fixture expectations and non-claim labels are preserved. It does not upgrade parser, retrieval, FalkorDB, relation, ETL, product graph, or legal-answer readiness.

## Failure diagnostics surface

Future agents should diagnose failures by rerunning:

```bash
uv run python scripts/evaluate-parser-golden-cases.py --check
```

The evaluator emits compact JSON with `status`, `case_count`, `evaluated_case_count`, `severity_counts`, `blocked_claims`, `non_authoritative`, and path-qualified diagnostics. Diagnostics intentionally point to tracked paths such as `prd/parser/odt_source_block_records.jsonl`, `prd/parser/consultant_relation_candidates.jsonl`, and `prd/parser/parser_staging_graph.json` without echoing raw full legal text, credentials, external URLs, or large JSON dumps.

## Downstream handoff

Use this report as a cold-reader proof that R032 has executable golden-test evidence for the current tracked artifacts only. Any future claim about parser completeness, retrieval quality, citation-safe answers, legal correctness, relation correctness, FalkorDB runtime loading, product ETL readiness, or product graph truth requires a separate proof slice and fresh executable evidence.
