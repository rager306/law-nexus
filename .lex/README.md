# .lex/

Knowledge graph managed by git-lex.
Kit: repolex-ai/git-lex-kit-base

- `extract/` — extraction sidecars (.spo)
- `ontology/` — ontology definitions
- `.git/lex/oxigraph/` — local SPARQL store
