# Local/runtime agent state
.agents/skills/
.bg-shell/
.gsd/
.claude/
.artifacts/
.commandcode/
CLAUDE.md
AGENTS.md

# Historical vaults — not active product truth (R066 / ADR-0014 era / D104).
# Trees may remain on disk for human archaeology; they must not be git-tracked
# or treated as GitNexus/active-governor source surfaces. Use
# `git rm -r --cached <path>` if still tracked, then keep ignore entries.
.lex/
python_archive/
Old_project/

# Historical PRD/ACP vault (relocated from active prd/ paths). Keep the vault
# README tracked if present under a non-ignored path; deep trees are ignored to
# reduce index noise. Use `git add -f prd/archive/README.md` when updating.
prd/archive/acp-git-lex/
prd/archive/pre-rust-prd/

# Retired active-plane aliases/proofs. Canonical copies live only in ignored
# archive vaults; these exact paths must not be recreated or indexed.
prd/architecture/acp
scripts/s02-cold-path-install-proof.sh
scripts/s02-first-proof.sh
scripts/s02-self-check.sh
scripts/s03-install-rehearsal-workflow-proof.sh
scripts/s03-validate-reject-proof.sh
tests/test_consultant_parser_proof.py
tests/test_hierarchy_metadata_completeness.py
tests/test_local_retrieval_runtime_check_cli.py
tests/test_source_structuring_protocol.py

# Historical noise vault (skills/scripts/tests). Entire tree ignored.
archive/

# Removed isolated storage probe packages (do not re-add)
probes/

# Generated skill verification reports
.agents/skills/falkordb/evals/*-report.json
.agents/skills/falkordb/evals/*-report.md
.agents/skills/falkordb/evals/trigger-proxy/

# Generated ConsultantPlus source-discovery runtime outputs
law-source/consultant/runtime/

# Environment / secrets
.env
.env.*
!.env.example

# OS/editor noise
.DS_Store
*.swp
*.swo

# Litho / deepwiki-rs local config, derived wiki + cache (non-authoritative; see doc/litho-runbook.md)
litho.toml
.litho/
litho.docs/

# Dependencies and build outputs
node_modules/
dist/
build/
.next/
coverage/

# ── GSD baseline (auto-generated) ──
.gsd
.gsd-id
.mcp.json
Thumbs.db
*~
.idea/
.vscode/
*.code-workspace
__pycache__/
*.pyc
.venv/
venv/
target/
vendor/
*.log
.cache/
tmp/

# ── GSD baseline (auto-generated) ──
nul
nul.*
con
con.*
prn
prn.*
aux
aux.*
com[1-9]
com[1-9].*
lpt[1-9]
lpt[1-9].*

# ── GSD baseline (auto-generated) ──
.gsd-worktrees/

# ── GSD baseline (auto-generated) ──
.gsd-backups/
*.egg-info/

prd/archive/milestone-proofs-era/
prd/archive/research-era/
prd/archive/project-state-era/
prd/archive/architecture-era/
prd/archive/parser-dumps-era/
prd/archive/retrieval-era/
prd/archive/migration-era/
prd/research/
prd/retrieval/
prd/milestone_proofs/
consru_export.zip
*.zip
*.tabby-upload
